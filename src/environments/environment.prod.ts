export const environment = {
  production: true,
  apiUrl: 'https://api-v3.igdb.com/', // for now, this api doesn't have the cors header, so you need to make a reverse proxy server.
  apiToken: '<INSERT YOUR TOKEN HERE>' // https://www.igdb.com/users/new
};
